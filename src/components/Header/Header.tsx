"use client";
import React from "react";
import { HeaderContainer } from "./styled";

const Header: React.FC = () => {
  return <HeaderContainer>Meu Blog</HeaderContainer>;
};

export default Header;
